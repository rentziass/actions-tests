name: test race
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: [ self-hosted-rentziass ] 
    steps:
      - name: Run a one-line script
        run: echo Hello, world!
  test:
      needs: [build]
      runs-on: [ self-hosted-rentziass ]
      steps:
        - name: Run a one-line script
          run: echo Hello, world!
  
        # Runs a set of commands using the runners shell
        - name: check attempt
          if: github.run_attempt == 1
          run: |
            exit 1
